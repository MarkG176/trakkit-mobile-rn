import { colors } from '@/theme/tokens';

const Colors = {
  light: {
    text: colors.foreground,
    background: colors.background,
    tint: colors.primary,
    tabIconDefault: colors.secondaryForeground,
    tabIconSelected: colors.primary,
  },
  dark: {
    text: colors.foreground,
    background: colors.background,
    tint: colors.primary,
    tabIconDefault: colors.secondaryForeground,
    tabIconSelected: colors.primary,
  },
};

export default Colors;
