EXPO_PUBLIC_SUPABASE_URL=https://skafzkzjaszxgqryzhjp.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Expo push notifications (required for getExpoPushTokenAsync):
#   Run `npx eas login` then `npx eas init` and paste the project UUID here.
#   Production Android also needs FCM credentials uploaded in the Expo/EAS project.
EXPO_PUBLIC_EAS_PROJECT_ID=

# Supabase Auth → URL Configuration → Redirect URLs (add all that apply):
#   trakkit://callback
#   trakkit://**
# Google OAuth client ID/secret are configured in Supabase Dashboard → Authentication → Providers → Google.
